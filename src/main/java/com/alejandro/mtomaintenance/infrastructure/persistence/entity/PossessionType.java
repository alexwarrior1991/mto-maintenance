package com.alejandro.mtomaintenance.infrastructure.persistence.entity;

/** Posesion parcial (una via cortada entre dos seccionadores, la contigua en tension) o total (fin de semana y festivos). */
public enum PossessionType {
    PARTIAL,
    FULL
}
